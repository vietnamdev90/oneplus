################################################################################
## Inheriting configs from ACK
. ${ROOT_DIR}/common/build.config.common
. ${ROOT_DIR}/common/build.config.aarch64

################################################################################
## Variant setup
MSM_ARCH=canoe_tuivm
VARIANTS=(defconfig debug-defconfig)
[ -z "${VARIANT}" ] && VARIANT=debug-defconfig

if [ -e "${ROOT_DIR}/msm-kernel" -a "${KERNEL_DIR}" = "common" ]; then
	KERNEL_DIR="msm-kernel"
fi

BOOT_IMAGE_HEADER_VERSION=3
BASE_ADDRESS=0x80000000
PAGE_SIZE=4096
VM_DTB_IMG_CREATE=1
PREFERRED_USERSPACE=vm

declare -A MKDTIMGDTBS

################################################################################
## Define TUI VM parameters
TUIVM_KERNEL_OFFSET=0x200000
TUIVM_DTB_OFFSET=0x2200000
TUIVM_RAMDISK_OFFSET=0x2300000
TUIVM_CMDLINE_CPIO_OFFSET=0x2300000
TUIVM_METADATA_OFFSET=0x0
TUIVM_METADATA_SIZE=8192

MKDTIMGDTBS["canoe_tuivm"]="canoe-vm"

################################################################################
## Define OEM VM parameters
OEMVM_KERNEL_OFFSET=0x200000
OEMVM_DTB_OFFSET=0x2200000
OEMVM_RAMDISK_OFFSET=0x2300000
OEMVM_CMDLINE_CPIO_OFFSET=0x2300000
OEMVM_METADATA_OFFSET=0x0
OEMVM_METADATA_SIZE=8192

MKDTIMGDTBS["canoe_oemvm"]="canoe-oemvm"

VM_SIZE_EXT4=270000000
DUMMY_IMG_SIZE=4096

################################################################################
## Inheriting MSM configs
. ${KERNEL_DIR}/build.config.msm.common
. ${KERNEL_DIR}/build.config.msm.vm
