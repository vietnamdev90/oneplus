# SPDX-License-Identifier: GPL-2.0
#
# Kbuild file for Wonder WiFi driver
# This file specifies additional symbol dependencies

# Include Makefile.include to get KBUILD_EXTRA_SYMBOLS configuration
-include $(src)/Makefile.include

obj-m += wonder.o
wonder-y := main.o mac80211.o wondertap.o nl80211_ven_cmd.o mac80211_txs.o ssr.o band_config.o

ifneq ($(CONFIG_DEBUG_FS),)
wonder-y += debugfs.o
endif
